[project]
name = "dnd-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "google-genai>=1.66.0",
    "openai>=2.26.0",
    # Unpinned on purpose: the browser runner resolves prompt_build_tool from
    # PyPI at page load, so the server tracks the same latest release rather
    # than drifting from what deployed users actually run.
    "prompt-build-tool",
    # Swap the line above for this one to run the server against a local pbt
    # checkout (uv sync after switching; uv lock --upgrade-package
    # prompt-build-tool to go back to the latest release):
    # "prompt-build-tool @ file:///Users/conradbez/Documents/prompt-build-tool-for-LLMs",
    "python-multipart>=0.0.22",
    "uvicorn>=0.41.0",
]
