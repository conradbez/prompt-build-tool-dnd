todo layout.md

new nodes should top align with other child nodes to the right of them
enable grid layout on grapoh node (ideally built in if exisit otherwise drop to pixle closeeest at half width of node block width x,y )
look at workflowy again (https://workflowy.com/ open in chrome), subtlke shift left right can change the level of drop (on dragging a bullet) - see where this activates and implement